[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flappy-rf"
version = "0.1.0"
description = "Flappy Bird AI with custom CART and Random Forest from scratch"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]